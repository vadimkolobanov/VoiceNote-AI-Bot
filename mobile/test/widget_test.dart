// Placeholder — реальные widget/unit-тесты добавим отдельно.
// Boilerplate от `flutter create` был удалён потому что ссылался на
// несуществующий MyApp.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke — harness reachable', () {
    expect(1 + 1, 2);
  });
}
